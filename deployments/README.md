# Deployment records

The production Battery Passport deployment is recorded in [`mainnet.json`](mainnet.json).

Only verified public Mainnet identifiers belong here. If a deployment transaction
or current admin is not independently verified, leave it unset rather than guessing.

Never commit secret keys, seed phrases, recovery phrases, signing XDR or private RPC credentials.